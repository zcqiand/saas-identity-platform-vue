# saas-identity-platform-vue — 开发期环境变量模板
#
# CLAUDE.md 硬约束：禁止把后端模式写到 .env（runtime 切换必须不需要 rebuild）。
# 所以本仓 .env 只放 dev port；后端 URL 走 backend-config.ts 模块级默认值 +
# BackendSwitcher UI 运行时覆盖。

# Vite dev server 端口
VITE_DEV_PORT=5173

# —— 跨 origin 提示 ——
# saas-vue :5173 dev → 后端 :5000（ASP.NET）/ :8080（Spring）
# 后端 CORS 已允许 http://localhost:5173，无需 .env 配
# 如部署到非 localhost，加 SAAS_BACKEND_ORIGINS（未来）

# —— 生产 build ——
# 部署平台（Vercel / Netlify / 自托管）设置同名变量覆盖 VITE_DEV_PORT