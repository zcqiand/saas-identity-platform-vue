# ch42 全栈 Nginx 配置：前端 SPA + API 反向代理
# 前端静态资源 root /var/www/frontend，API 代理到 backend:8080
# 与 React 双栈仓 deploy/nginx.conf 同构（双栈对照）。

server {
    listen 80;
    server_name _;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1024;

    # 前端 SPA 静态资源（root 指向 dist 产物）
    location / {
        root /var/www/frontend;
        index index.html;
        # SPA history 模式回退：未匹配静态文件时回到 index.html
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到后端服务
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SSO 授权服务器代理
    location /sso/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 静态资源缓存（带 hash 的 js/css 永久缓存）
    location /assets/ {
        root /var/www/frontend;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # index.html 不缓存（保证发布后立即生效）
    location = /index.html {
        root /var/www/frontend;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 错误上报接收（ch42 errorHandler 上报通道）
    location /api/vitals {
        proxy_pass http://backend:8080;
        proxy_set_header Content-Type application/json;
    }
}
