# saas-identity-platform-vue 生产环境变量(Vite build-time 烘焙)

# 后端地址 —— saas 家族 vue 系列前端默认 → saas-aspnetcore (port 8024)
# （跨仓约定：react → springboot, vue → aspnetcore, 见 CLAUDE.md §1）
# 之前误写 saas-nextjs.xiangru.uk — 但 saas-nextjs 仓无 /api/v1/auth/login 路由
# （nextjs 是另一套前端 + 它自己的 API routes，不是 saas 后端的代理）。
# 正确做法：VPS nginx 把 saas-aspnetcore.xiangru.uk 反代到 127.0.0.1:8024 (容器内 :8080)。
VITE_API_BASE_URL=https://saas-aspnetcore.xiangru.uk
VITE_API_MODE=aspnetcore
# dev server 端口契约(L0.5:三份 key 集合一致;prod/test 不参与 dev server,占位 dev 值)
VITE_DEV_PORT=5173
