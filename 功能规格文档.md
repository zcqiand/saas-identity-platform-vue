# saas-identity-platform-vue — 功能规格文档

> 本文档描述 saas-identity-platform-vue 的所有功能特性、验收标准与测试策略。
> 与 [CLAUDE.md](CLAUDE.md) 共同构成本仓的完整规范文档。

## 功能清单

### 多租户架构

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 租户 store | tenantStore 管理 init/switch/resolveTenantIdFromPath + subscribedFeatures | 切换租户后所有 API 请求携带正确的 X-Tenant-ID |
| 动态路由 dynamicRoutes | setupDynamicRoutes/teardownDynamicRoutes 按 feature addRoute | 租户切换时路由动态增减 |
| 路由守卫 beforeEach | 导航守卫解析租户上下文（第二层守卫做权限校验） | 未登录跳转 /login；无权限跳转 /403 |
| 租户主题 | useTheme composable 提供 applyTheme/clearTheme + provide/inject | 租户切换时主题正确应用与清除 |
| axios 拦截器 | 请求拦截器注入 X-Tenant-ID | 所有 /api/* 请求均携带租户头 |

### 统一认证（SSO/OAuth2）

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| SSO 跳转 | useSso composable 提供 buildSsoRedirectUrl/handleSsoCallback/generateState | MSW mock /sso/authorize 端点返回 302 回调 |
| OAuth2 多 Provider | useOAuth composable 支持 5 种 OAuth provider | buildOAuthAuthorizeUrl 正确拼接各 provider 参数 |
| 授权回调 | handleOAuthCallback 接收 code，换 token | MSW 签发 mock access_token 并写入 auth store |
| 登录流程 | loginWithSso/refreshPermissions/switchOrg/logout | auth store 正确管理会话状态 |

### RBAC 权限管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| v-permission 指令 | 按权限码移除无权限元素（removeChild，非 v-if） | 无权限元素从 DOM 移除 |
| usePermission composable | hasPermission/hasRole 等脚本侧判定 | 路由守卫正确使用该 composable |
| 权限矩阵 | Role × MenuPermission 矩阵配置 | 类型定义 RolePermissionMatrix 与 React 仓一致 |

### 用户与组织管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| OrgTreeNode 递归组件 | Vue 递归自引用组织树组件 | 支持展开/折叠/选中 |
| UserList | useTable composable 分页/搜索 + 角色分配 | 搜索防抖 300ms；分页参数正确传递 |
| 审计日志 AuditLog | shallowRef + useVirtualList 虚拟滚动 + action 过滤 | AuditLog.vue 超过 100 项时仅渲染可视区域 |

### 虚拟滚动

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| useVirtualList composable | 定高虚拟窗口实现 | AuditLog 列表滚动流畅，无白屏/闪烁 |
| 虚拟滚动集成 | AuditLog.vue 使用虚拟滚动 + shallowRef | 与 React 仓 react-window 功能对齐 |

### 可复用 composables

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| useSso | SSO 跳转 URL 构建/回调处理/state 生成 | 复用至所有 SSO 场景 |
| useOAuth | OAuth 授权 URL 构建/回调处理 | 支持 5 种 provider |
| usePermission | hasPermission/hasRole/hasAnyPermission 权限判定 | 路由守卫与指令共同依赖 |
| useTheme | 主题 apply/clear + provide/inject | 租户切换时主题正确切换 |
| useTable | 分页/搜索/排序/防抖（300ms） | 多列表页复用 |
| useResource | 通用 list/create/update/remove，含 loading/error | 与 React 仓 useResource 对齐 |

### 部署与监控

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 全局错误捕获 | app.config.errorHandler + import.meta.env 错误上报 | 错误路由 /error 可访问 |
| MSW 浏览器 worker | src/main.ts 仅在 DEV 且 VITE_OFFLINE !== '0' 时启动 | 生产构建静态消除 MSW worker 代码 |
| nginx SPA fallback | deploy/nginx.conf try_files history 回退 + /api、/sso 反代 | 刷新路由 /tenants/:id 等正确返回 index.html |
| Lighthouse 目标 | Performance/Accessibility/Best Practices/SEO 均 > 90 | lighthouse 评分达标 |

### 测试策略

| 层级 | 目录 | 说明 |
| :--- | :--- | :--- |
| 单元测试 | `tests/stores/` `tests/composables/` `tests/views/` `tests/directives/` | Pinia store、composable、Vue 组件、指令隔离测试 |
| 集成测试 | `tests/integration/` | MainLayout + Pinia + router 全链路 |
| MSW handler 测试 | `tests/msw/` | API 契约校验 |

## 覆盖率阈值

- Lines / Statements / Functions: 80%
- Branches: 75%

## 快速链接

- [README.md](README.md) — 项目介绍与快速开始
- [CLAUDE.md](CLAUDE.md) — 开发约定与编码规范
